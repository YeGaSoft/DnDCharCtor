@attribute [Route(Routes.EditCharacter)]
@page "/editCharacter/{id}"

@inject EditCharacterViewModel ViewModel
@inject NavigationManager NavigationManager

<CascadingValue Name="@CascadeValueNames.DataContext" Value="@ViewModel">
    <FluentGrid AdaptiveRendering="true" Style=@($"""background-color: var(--neutral-layer-3); padding: 4px; {(ViewModel.HasValidationErrors ? "border-color: red; border-width: medium;" : "")}""")>
        <FluentGridItem xs="12" sm="6" md="4" lg="3" xl="2">
            <PersonalityCard PersonalityExpression="@(() => ViewModel.CharacterViewModelToEdit.PersonalityViewModel)" />
        </FluentGridItem>
    </FluentGrid>
    <br />
    <FluentDivider />
    <br />
    <FluentStack VerticalAlignment="VerticalAlignment.Center" HorizontalAlignment="HorizontalAlignment.Center">
        <FluentButton OnClick="SaveChanges" Appearance="Appearance.Accent" IconStart="@(new Icons.Regular.Size20.Save())">Save</FluentButton>
    </FluentStack>

</CascadingValue>
